export const CLOCK_CONSTANTS = {
  CENTER_OFFSET: 50, // Center position for clock number
  RADIUS: 45, // Distance of number from center
  DEGREES_PER_HOUR: 30, // Defrees moves by the hour hand per hour
  DEF_TO_RAD: Math.PI / 180, // Convert degrees

  DEGREES_PER_MINUTE_SECOND: 6, // Degrees moved per minute or second
  MINUTE_ADJUSTMENT: 0.5, // Hour hand adjustment per minute
  SECOND_ADJUSTMENT: 0.1, // Minute hand adjustment per second
  OFFSET_ROTATION: 180, // Rotation offset for alignment
};
